---
# Ansible Galaxy requirements for playbooks
# Installs required collections before playbook execution
# Note: Local roles are automatically found via roles_path in ansible.cfg

collections:
  - name: community.general
    version: ">=5.0.0"
